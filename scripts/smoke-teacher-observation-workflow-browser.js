// The contact/profile smoke owns the shared fixture because this workflow begins on
// the same student detail surface and verifies contact, observation, risk review,
// and the global teacher-report queue in one browser session.
require('./smoke-student-contact-browser');
